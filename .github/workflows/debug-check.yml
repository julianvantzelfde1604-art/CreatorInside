name: Debug Check

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Try the request and capture everything about the response
        shell: bash
        run: |
          echo "=== Apify Hashtag Scraper with resultsLimit=150 ===" > debug-output2.txt
          curl -s -X POST "https://api.apify.com/v2/acts/reGe1ST3OBgYZSsZJ/run-sync-get-dataset-items?token=${{ secrets.APIFY_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"hashtags":["parentingtips"],"resultsLimit":150}' \
            -o response_body.json
          python3 -c "
import json
with open('response_body.json') as f:
    data = json.load(f)
print('Items returned:', len(data) if isinstance(data, list) else 'not a list -- ' + str(data)[:300])
if isinstance(data, list) and len(data) > 0:
    unique_owners = set(p.get('ownerUsername') for p in data if p.get('ownerUsername'))
    print('Unique usernames found:', len(unique_owners))
" >> debug-output2.txt
          cat debug-output2.txt

      - name: Push the result so it can be read back
        shell: bash
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git fetch origin
          git checkout -B debug-output2
          git add debug-output2.txt
          git commit -m "Debug output 2"
          git push -f origin debug-output2
